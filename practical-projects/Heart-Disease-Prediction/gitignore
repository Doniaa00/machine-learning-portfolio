# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
*.so

# Virtual environments
venv/
env/
.env/
.venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# VS Code / IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs and temp files
*.log
*.tmp
*.bak
*.swp

# Data — exclude large raw files if needed
# heart.csv  ← Keep this tracked (small dataset, needed to run notebook)

# Model artifacts (optional — uncomment to exclude large .pkl files)
# model/*.pkl

# Output images generated by notebook (optional)
# images/
